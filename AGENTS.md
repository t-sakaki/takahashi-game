# AGENTS.md

## ガイドライン（AI エージェント向け）

このリポジトリに貢献する際は、以下の点に留意してください。

### コーディングスタイル
- JavaScript は 2 スペースインデント、セミコロン必須。
- CSS も 2 スペースインデント、プロパティはアルファベット順に並べることを推奨。
- 関数は小さく、単一責任を持たせる。
- 複雑なロジックにはコメントを追加し、なぜその実装にしたのかを説明する。

### アクセシビリティ
- セマンティック HTML を使用し、可能な限り `<header>`, `<nav>`, `<main>`, `<section>`, `<footer>` を活用。
- インタラクティブな要素には適切な `aria-label` または `aria-labelledby` を付与。
- 色コントラストは WCAG AA 以上を確保（テキストと背景の比率 4.5:1 以上）。
- キーボードのみで全操作が行えるように、`tabindex` とフォーカススタイルを確保。

### テストとコミット
- 変更は必ずローカルで動作確認（`python -m http.server` 等）後、コミットする。
- コミットメッセージは日本語で簡潔に（例: `feat: ゲーム1のバランス調整`、`fix: ボタンのフォーカスリング修正`）。
- 変更が機能追加またはバグ修正の場合は、`TODO.md` の該当項目をチェックしてください。
- ドキュメント（README、DEPLOYMENT など）も必要に応じて更新する。

### ブランチ運用
- `main`（または `master`）は常にリリース可能な状態を保つ。
- 機能追加は `feature/*` ブランチで作業し、レビュー後に `main` へマージ。
- バグ修正は `fix/*` ブランチで行い、同様にプルリクエストを送る。

### ライセンス
- 本プロジェクトは CC0 1.0 Universal（パブリックドメイン）です。新規追加するアセット（画像・音声・フォント等）も可能な限り CC0 または CC‑BY 互換ライセンスを使用し、`LICENSE` ファイルに明記してください。

---

以上のガイドラインに従って、快適に開発を進めてください。